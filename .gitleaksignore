# Findings the secret scanner should stop re-reporting.
#
# FINGERPRINTS ONLY — never the secret itself. A fingerprint is
# commit:file:rule:line, so this file identifies WHICH finding is handled
# without writing the value into a tracked file, which is what an
# allowlist-by-value would do.
#
# POLICY (owner, 2026-08-02): every key is treated as a secret, including
# Supabase "publishable"/anon keys. They stay out of git regardless of what
# the vendor says is safe to expose. An entry here is never a claim that a
# value is harmless — it means the value has been ROTATED and the dead
# original cannot be scrubbed from history that is already pushed.

# flashml-dev Supabase publishable key, committed in .env.dev.example on
# 2026-08-02 and pushed before the policy above was set.
#
# !! ROTATION PENDING — owner action. !!
# The key has NOT been rotated yet; there is no API for it, so it has to be
# done in the Supabase dashboard (flashml-dev -> Settings -> API keys ->
# rotate/disable the publishable key). Until that happens the value in history
# is still live. Nothing depends on it yet: the dev project is new, the
# template now leaves the field blank, and render.yaml carries it as
# `sync: false` like prod.
#
# This entry exists so CI is not permanently red on a value that cannot be
# scrubbed from already-pushed history. It is NOT a statement that the value
# is safe.
b1e5df96aa7c76021fb1be5a5fe538a249bedcde:.env.dev.example:generic-api-key:32

# fake invite-token literal in web tests (6bc4218) — fabricated value, never a real credential
6bc4218192b3df87514db38fc6f7a0b9cec8320d:flashml-cloud/apps/web/lib/cloud-api.test.ts:generic-api-key:317
6bc4218192b3df87514db38fc6f7a0b9cec8320d:flashml-cloud/apps/web/lib/cloud-api.test.ts:generic-api-key:325
6bc4218192b3df87514db38fc6f7a0b9cec8320d:flashml-cloud/apps/web/lib/cloud-api.test.ts:generic-api-key:344

# --------------------------------------------------------------------------
# FALSE POSITIVES — a category the policy above did not have.
#
# The note at the top says an entry means "the value has been ROTATED". These
# two do NOT mean that, and conflating them would be the dangerous direction:
# a future reader must be able to tell a dead-but-real key from a value that
# was never a secret at all.
#
# Both findings are the SAME string, `token_prefix: "fmu_abc12345"`, in a test
# fixture and in the plan document that quotes it. A `token_prefix` is by
# construction the non-secret head of a credential — it is what the console
# shows a user so they can tell two CLI tokens apart — and `abc12345` is
# placeholder text. There is nothing to rotate.
#
# gitleaks fires on the `fmu_` prefix looking like a vendor key prefix, which
# is a reasonable heuristic and a wrong call here.
85a74c8eebf67956bdf28e0b311348599c0a09e2:flashml-cloud/apps/web/lib/cli-credential-status.test.ts:generic-api-key:9
a174991482e9823894ccaf42131c012f9164c6a2:flashml-cloud/docs/superpowers/plans/2026-08-10-developer-identity.md:generic-api-key:1724
