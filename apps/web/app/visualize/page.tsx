import { VisualizeResults } from "@/components/viz/VisualizeResults";

export const metadata = {
  title: "Visualize Results | FlashML",
};

export default function VisualizePage() {
  return <VisualizeResults />;
}
