"""FlashML Cloud control-plane API (private).

Node registry + heartbeats, user job submission (delegated to the
FlashRuntime API), event timelines, artifact links, and deployment-profile
status for the dashboard. All wire schemas come from flashruntime.protocol.
"""

__version__ = "0.1.0"
