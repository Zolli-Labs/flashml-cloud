// Settings · GitHub. The page component is unchanged; /account/github
// redirects here (next.config.ts). The OAuth callback stays at
// /account/github/callback — that URL is registered with GitHub.
export { default } from "../../account/github/page";
