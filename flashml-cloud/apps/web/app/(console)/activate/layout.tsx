import type { Metadata } from "next";

export const metadata: Metadata = { title: "Activate a machine" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
