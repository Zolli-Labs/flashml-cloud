import type { Metadata } from "next";

export const metadata: Metadata = { title: "Activate a Zolli" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
