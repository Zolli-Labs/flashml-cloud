// The activation flow as the Machines section's second tab. /activate
// redirects here (next.config.ts) with its query string — the ?pool=
// parameter on activation links is load-bearing — and the page component
// itself is unchanged.
export { default } from "../../activate/page";
