Status: done — migration + tests written, NOT applied to Supabase (per instructions).
Commit: 617960ebcf03a8f0bb414d9610efc61e5cd44dab
Tests before (whole apps/api suite): 7 passed
Tests after (whole apps/api suite): 14 passed (7 pre-existing + 7 new in test_schema.py)
test_schema.py: confirmed FAIL (file missing) before writing SQL, 7 passed after.
SQL I'm unsure about: none functionally, but I have not run it against a real
Postgres — `gen_random_uuid()` assumed built-in on PG13+/Supabase (no
pgcrypto needed) but I added `create extension if not exists pgcrypto;`
defensively anyway; worth confirming that's harmless on Supabase (it should
be a no-op if already present). Recommend validating via apply_migration as
planned.
