Status: Tasks 5–7 complete. `npm run build` clean, `npm test` 8/8 passing.

Commits (branch `local-milestone-2026-07`):
- a95005b — Task 5: `/submit` rewritten against `POST /v1alpha1/jobs/from-repo`.
- 578795a — Task 6: `/jobs`, `/jobs/[jobId]`. Note: a concurrent fork of mine
  (dispatched for read-only flashruntime research) pre-staged part of Task 7
  unprompted; my `git commit` swept its staged files in too, so this commit
  also contains `lib/api.ts` + `/dashboard` + `/launch` + `/visualize` deletion.
- b44c525 — Task 7: remaining cleanup (`/nodes`, `/integration`, `lib/poc-api.ts`,
  `MetricCard.tsx`), dead-link fixes (Hero/HowItWorks/Navbar → `/submit`,`/jobs`),
  README update.

Bundle secret scan: grepped `.next/static` for service-role key, JWT secret,
operator-token strings — none found. Only `NEXT_PUBLIC_CLOUD_API`,
`NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_SUPABASE_URL` appear.

Worked around / judgment calls:
- Preflight warnings never block server-side (`preflight.py`: "a warning is
  advice that does not block"), so there's no dry-run to gate a literal
  "submit anyway" button on. The post-submit screen shows warnings in full
  and requires a click to view the job instead of a silent redirect.
- Per-round federated-averaging progress (round/participants/mean-loss) and
  "which machines contributed" are NOT rendered — verified against
  `flashruntime.protocol.v1alpha1.JobRecord` and `apps/api`'s `jobs` table
  that neither carries this yet (the driver exists in flashruntime but isn't
  invoked by apps/api). Omitted per "don't invent metrics."
- Deleted `/nodes` and `/integration` beyond the plan's named candidate list:
  both hit `apps/api`'s legacy pre-accounts mode, unreachable from this
  auth'd app; kept as unlinked dead pages they'd have looked like live
  features.
- No live Supabase/Google OAuth or cloud API in this sandbox — verified the
  375px flow with a throwaway (uncommitted, gitignored) `.env.local` against
  the real project URL + a placeholder anon key: no horizontal overflow on
  `/sign-in`, `/submit`, `/jobs`; every private route redirects to
  `/sign-in` both via middleware and via `NotAuthenticated` client-side.
  A real authenticated walk needs live infra this environment doesn't have.
