Status: fixed and committed (also fixed `render.yaml`'s `NEXT_PUBLIC_API_BASE` -> `NEXT_PUBLIC_CLOUD_API` — the code never read the former, so Bug 2's fix would have been dead in prod).
Commit SHA: 7645d0e

API tests: 339 passed, 0 skipped (baseline 329 + 10 new in `tests/test_settings.py`)
Web tests: 13 passed (baseline 10 + 3 new); `npm run build` clean

Explicit-scheme preservation confirmed unchanged:
- Python: `http://x` and `https://x` pass through `_with_default_scheme` unchanged (`test_from_env_leaves_an_explicit_http/https_scheme_unchanged`).
- TypeScript: `http://localhost:8000` and `https://cloud.example.com` pass through `withDefaultScheme` unchanged, verified against the actual outbound `fetch()` URL.
