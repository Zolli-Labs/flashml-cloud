Status: done
Commit: de88a737f5ffce8c4bcfe2024049477c0e63d816
Build: `npm run build` — clean (Next.js + TS compile, no errors)
Test: `npm test` — 10/10 passed (8 existing + 2 new for listJobRounds)
Secret scan: `grep -rniE "service_role|SUPABASE_SERVICE|JWT_SECRET|OPERATOR_TOKEN" .next/static` — zero matches
No-rounds job: `listJobRounds(jobId)` is fetched alongside `getJob`; state
`rounds` defaults to `[]` and the "Rounds" card renders only when
`rounds.length > 0`, so independent jobs show nothing (no empty/pending
section). `mean_loss: null` renders as "loss —" and gets no trend bar,
never 0 or an interpolated value. Job-status polling and rounds polling
share one `load()` call, so both stop together at a terminal job state.
