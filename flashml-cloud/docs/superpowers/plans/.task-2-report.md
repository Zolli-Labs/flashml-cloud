Status: done — settings.py, auth.py, and test_auth.py written and passing.
Commit: 2785e28888df2218bc0103b4787b227e8ba0a0b2
Tests before: 14 passed
Tests after: 22 passed (14 pre-existing + 8 new in test_auth.py)
test_auth.py confirmed FAIL first (ModuleNotFoundError: jwt / flashml_cloud_api.auth), then 8 passed.
Confirmed: jwt.decode is called with algorithms=["HS256"] explicitly (never
trusting a token-declared alg) plus audience="authenticated"; alg=none
forgery test passes.

--- Follow-up: missing-exp hole (coordinator-reported) ---
Status: fixed.
Commit: 25b1e884382acb6b92582156b89a42ed65ebf52b
Tests before this fix: 22 passed
Tests after this fix: 25 passed (added test_token_without_exp_is_rejected,
test_token_without_sub_is_rejected, test_token_without_aud_is_rejected)
Fix: jwt.decode now passes options={"require": ["exp", "sub", "aud"]}.

Pre-fix run of the three new tests (against unfixed auth.py):
  test_token_without_exp_is_rejected FAILED — DID NOT RAISE AuthError
    (confirms the real hole: PyJWT validates exp only if present)
  test_token_without_sub_is_rejected PASSED (already guarded by the
    existing `if not sub: raise AuthError` check)
  test_token_without_aud_is_rejected PASSED (already guarded — PyJWT
    raises InvalidAudienceError when audience= is requested but the
    token has no aud claim)
  -> 1 failed, 2 passed, 8 deselected
Post-fix: all three pass; full suite 25 passed.
