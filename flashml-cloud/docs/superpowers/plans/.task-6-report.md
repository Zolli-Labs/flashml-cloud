Status: done. Plan 3 (cloud API + Supabase) is now complete.
Commit (flashml-cloud, apps/api code+tests): ddfd6a1
Commit (workspace root, M1_DECISIONS.md D14 + PROGRESS.md): c55f265
Tests before: 75 passed, 0 skipped. After: 91 passed, 0 skipped (16 new in
tests/test_jobs.py). Zero-skip confirmed via `pytest -q -rs` (no skip lines).
Upstream re-check (unaffected by this task, confirmed no regression):
flashruntime 532 passed/1 skipped/20 deselected, flashnode 85 passed/1
skipped, e2e 15 passed.

Adversarial findings:
- owner_id is written only from `Depends(current_user)` (verified JWT sub);
  a body-supplied owner_id/owner is parsed and discarded — pinned by a test
  reading the DB row directly after a spoofed submission.
- Get-by-id and cancel check DB ownership *before* any coordinator call;
  cancel-not-called is asserted on the fake transport's request log, not
  just the status code. Both routes return 404, never 403, and the "other
  user's job" vs "nonexistent id" responses are asserted byte-identical.
- New job-scoped artifact route (GET /v1alpha1/jobs/{id}/artifacts/{key})
  closes the Task 5 residual; dot-segment keys rejected 400 pre-forward;
  machine tokens get 401 on all new browser job routes (checked explicitly).
- No route found that lets one user reach another's job data.
