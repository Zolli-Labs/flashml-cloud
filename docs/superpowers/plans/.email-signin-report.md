# Email sign-in — report

Status: done.
Commit SHA: 3a2adc346ea3553db7f512588272ca26a0403dd6
Build: `npm run build` — clean.
Tests: `npm test` — 10 passed (1 file), unchanged from before.
Auth callback (`app/auth/callback/route.ts`): no logic change needed —
`exchangeCodeForSession` already handles the PKCE code from both Google
OAuth and email-magic-link (`signInWithOtp` + `emailRedirectTo`) since both
flows converge on the same `code` param. Only the failure-redirect error
message was generalized (it previously named only Google).
Google button when unconfigured: `signInWithOAuth` returns a provider
error instead of redirecting; caught and shown as "Google sign-in isn't
set up for this deployment yet — use email above" (no raw provider error,
no silent no-op).
No service-role key or operator token found in `.next/static`.
