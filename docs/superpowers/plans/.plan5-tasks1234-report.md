Status: Tasks 1-4 complete. Build passes clean (`npm run build`, TS + generation, no errors). `npm test` (vitest, newly added) — 8/8 passing.

Commits (branch `local-milestone-2026-07`):
- 18bf804 — Task 1: Supabase auth (lib/supabase.ts, middleware.ts, sign-in, callback, nav sign-out)
- bcc2d1c — Task 2: lib/cloud-api.ts typed client + lib/cloud-api.test.ts (vitest)
- 514b2a5 — Task 3: app/activate/page.tsx
- eb49641 — Task 4: app/machines/page.tsx + MachineCard

No service-role key, JWT secret, or operator token appears anywhere in `.next/static` (grepped the built client bundle for `SUPABASE_SERVICE_KEY`, `SUPABASE_JWT_SECRET`, `COORDINATOR_OPERATOR_TOKEN`, `service_role` — the only hits are Supabase-SDK JSDoc warnings in server-only chunks, not values). Only `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`/`NEXT_PUBLIC_CLOUD_API` are read in browser code.

Worked around: `lib/api.ts`/`lib/poc-api.ts` were left untouched (Task 7 territory); `/activate` verified with a real 375px headless render (gstack `browse`) — found and fixed a pre-existing Navbar horizontal-overflow bug (`scrollWidth` 481 vs 375) by hiding nav links below `md`, since it broke every phone-width page including the new ones. `MachineCard`'s online/offline split is a documented client-side heuristic (90s since `last_seen_at`) since the API contract doesn't assert a heartbeat interval.
