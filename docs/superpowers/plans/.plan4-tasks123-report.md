# Plan 4, Tasks 1-3 — report

Status: DONE. All three modules built TDD, each with its own test file, committed separately.

Commits:
- Task 1 (images.py): b48890b
- Task 2 (flashml_yaml.py): b584655
- Task 3 (repo.py): e30d5f2

Test count after each task (`pytest -q -rs`):
- Baseline: 91 passed, 0 skipped
- After Task 1: 101 passed, 0 skipped
- After Task 2: 120 passed, 0 skipped
- After Task 3: 132 passed, 0 skipped

Zero-skip confirmation: `-rs` shows no skips at any point; final run is 132 passed, 0 skipped, 16 warnings (pre-existing HMAC-key-length warnings from test_auth.py, unrelated to this work).

Malicious-tarball surprise: `Path(dest) / "/etc/evil.txt"` in pathlib silently *discards* the left side and evaluates to `Path("/etc/evil.txt")` when the right operand is absolute — so the same `.resolve()` + `relative_to()` containment check catches both `../` zip-slip and absolute-path zip-slip for free, no special-casing needed. Also had to build the decompression-bomb fixture with a custom lazy `read()` file-like object (200 MB of zeros without ever materializing 200 MB in test memory) to prove the "check size as you extract" behavior without an actually-huge test.

Added `pyyaml>=6.0` to `apps/api/pyproject.toml` dependencies (was absent); installed via `uv pip install -e ".[dev]" --python .venv/bin/python`.
